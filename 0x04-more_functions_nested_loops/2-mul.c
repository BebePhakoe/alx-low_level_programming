#include "main.h"
#include <stdio.h>
/**
 * mul - Entry point
 * @a: First integer
 * @b: Second integer
 * Return: Always 0 (Success)
 *
 */

int mul(int a, int b)
{
	int results = a * b;
return (results);
}
